#COLLECTIONS
*collection é um objeto que agrupa múltiplos elementos (variáveis primitivas ou objetos) dentro de uma única unidade.

-Serve para armazenar e processar conjuntos de dados de forma eficiente

-Antes do java 2 (jdk 1.2), a implementação de coleções na linguagem java incluía poucas poucas classes e não tinha organização
de um *framework* 

#COMPOSIÇÃO DA COLLECTIONS:

- É um contrato que quando assumido por uma classe deve ser implementado

- Implementações ou Classes são as materializações, a codificação das interfaces

- Algorítimos : é uma sequência lógica, finita e definida de instruções para a resolução de um problema


#coleções com iterações ordenadas(list)

- Permite elementos duplicados e garante ordem de inserção

1-implementação Vector



2-implementação ArrayList (*array*)
   + usado onde mais operações de pesquisa são necessários (pesquisas e consultas de dados)


3-implementação LinkedList (*lista duplamente encadeada*)
   + usado onde mais operações de inserção e exclusão são necessários(manipulação de dados)
   
#  INTERFACE JAVA.UTIL.SET
- não permite elementos duplicados

- não possui indice

1-HashSet
  hashMap para armazenar elemetos
  não mantém a ordem dos elementos
  no maximo 1 null
  não possui métodos get e set
  
  

2-TreeSet
    treemap para armazenar elementos
	por default matém a ordem natural dos elementos ( crescente ou alfabética
	não permite null

3-LinkedHashset
  extende de hashset
  mantém a ordem dos elementos
  no maximo 1 null

# INTERFACE JAVA.UTIL.MAP 

* não estende da interface collection
* faz parte do framework

- Elementos únicos( key) para cada valor(value)
- não existem chaves repetidas, mas existem valores repetidos

-1 HashTable

-2 TreeMap (ordem natural das chaves)
-não permite chaves nulas

-3 HashMap
- permite chaves nulas



-4 LinkedHashMap ( estende da hashmap)
-permite chaves nulas

- mostra por ordem de inserção





